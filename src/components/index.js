import Vue from 'vue'
import ComponentContainer from './ComponentContainer'
import CodeContainer from './CodeContainer'

Vue.component(ComponentContainer.name, ComponentContainer)
Vue.component(CodeContainer.name, CodeContainer)
