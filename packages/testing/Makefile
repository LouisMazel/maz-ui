audit:
	npm audit

serve:
	npm run dev

lint-staged: ## lint-staged
	npm run pre-commit

install:
	npm i

clean:
	rm -rf node_modules

reinstall:
	make clean isntall

check-dependencies-updates:
	npx npm-check-updates --interactive --format group