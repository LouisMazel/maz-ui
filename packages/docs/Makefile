audit:
	npm audit

build:
	npm run build

serve:
	npm run dev

install:
	npm i

clean:
	rm -rf node_modules

reinstall:
	make clean install

check-dependencies-updates:
	npx npm-check-updates --interactive --format group

